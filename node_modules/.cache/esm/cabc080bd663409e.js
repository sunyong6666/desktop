_d66‍.x([["default",()=>_d66‍.o]]);global.code='123'
// let state={
//     code:'123'
// }

const codeModule = {
    getCode() {
      // console.log('hello');
      // console.log('CODE' + global.code);
      return global.code;
    },
    setCode(c) {
        global.code = c;
        // sendDataToServer(c)
    }
};

const sendDataToServer = (con) => {
  // 发送数据到服务器的函数
  fetch('http://localhost:3000/get-code', {
    method: 'POST',
    headers: {
      'Content-Type': 'text/plain'
    },
    body: con
  })
  .then(response => response.text())
  .then(data => {
    console.log('服务器响应:', data);
  })
  .catch(error => {
    console.error('错误:', error);
  });
};

_d66‍.d(codeModule);